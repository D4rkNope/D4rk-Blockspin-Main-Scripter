local function CreateButton(a, b)
	if type(a) ~= "string" then
		error("Image Error", 2)
	end
	if type(b) ~= "function" then
		error("Callback Error", 2)
	end
	if getgenv().OpenGuiButton then
		getgenv().OpenGuiButton:Destroy()
	end
	local c = Instance.new("ScreenGui")
	local d = Instance.new("Frame")
	local e = Instance.new("UICorner")
	local f = Instance.new("ImageButton")
	local g = Instance.new("UICorner")
	local h = Instance.new("UIScale")
	c.Parent = (gethui and gethui()) or game.Players.LocalPlayer:WaitForChild("PlayerGui")
	d.Parent = c
	d.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	d.BackgroundTransparency = 0.5
	d.BorderColor3 = Color3.fromRGB(0, 0, 0)
	d.BorderSizePixel = 0
	d.AnchorPoint = Vector2.new(0.5, 0.5)
	d.Position = UDim2.new(0.491, 0, 0, 65)
	d.Size = UDim2.new(0, 43, 0, 43)
	e.Parent = d
	f.Parent = d
	f.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	f.BackgroundTransparency = 1
	f.BorderColor3 = Color3.fromRGB(0, 0, 0)
	f.BorderSizePixel = 0
	f.Position = UDim2.new(0, 0, 0, 0)
	f.Size = UDim2.new(1, 0, 1, 0)
	f.Image = a
	g.Parent = f
	h.Parent = d
	h.Scale = 0
	local i = game:GetService("UserInputService")
	local j = game:GetService("TweenService")
	local k = false
	local l = 6
	local m = nil
	local n = nil
	local o = false
	task.defer(function()
		local p = d.AbsolutePosition + (d.AbsoluteSize / 2)
		d.Position = UDim2.new(0, p.X, 0, p.Y)
		j:Create(h, TweenInfo.new(0.6, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Scale = 1}):Play()
	end)
	f.InputBegan:Connect(function(p)
		if p.UserInputType == Enum.UserInputType.MouseButton1 or p.UserInputType == Enum.UserInputType.Touch then
			k = true
			o = false
			m = p.Position
			n = d.Position
			j:Create(h, TweenInfo.new(0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Scale = 0.85}):Play()
		end
	end)
	i.InputChanged:Connect(function(p)
		if not k then return end
		if p.UserInputType ~= Enum.UserInputType.MouseMovement and p.UserInputType ~= Enum.UserInputType.Touch then return end
		local q = p.Position - m
		if not o then
			if math.abs(q.X) < l and math.abs(q.Y) < l then return end
			o = true
		end
		j:Create(d, TweenInfo.new(0.13, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Position = UDim2.new(0, n.X.Offset + q.X, 0, n.Y.Offset + q.Y)}):Play()
	end)
	i.InputEnded:Connect(function(p)
		if p.UserInputType == Enum.UserInputType.MouseButton1 or p.UserInputType == Enum.UserInputType.Touch then
			k = false
			j:Create(h, TweenInfo.new(0.4, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out), {Scale = 1}):Play()
		end
	end)
	f.MouseButton1Click:Connect(function()
		if o then return end
		b()
	end)
	getgenv().OpenGuiButton = c
end

CreateButton("rbxassetid://107353997379031", function()
	if Window and Window.Toggle then
		Window:Toggle()
	end
end)
